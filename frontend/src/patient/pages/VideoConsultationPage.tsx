import React from 'react';

const VideoConsultationPage: React.FC = () => {
  return (
    <div>
      <h1>Video Consultation</h1>
      {/* Video call interface with audio-only fallback */}
    </div>
  );
};

export default VideoConsultationPage;
