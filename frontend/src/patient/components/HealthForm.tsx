import React from 'react';

interface HealthFormProps {
  // TODO: Add form props
}

const HealthForm: React.FC<HealthFormProps> = () => {
  return (
    <form className="health-form">
      {/* Pre-appointment health questionnaire fields */}
    </form>
  );
};

export default HealthForm;
