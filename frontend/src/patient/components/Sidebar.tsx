import React from 'react';

const Sidebar: React.FC = () => {
  return (
    <aside className="sidebar">
      {/* Dashboard navigation: Home, Appointments, Find Doctor, Settings */}
    </aside>
  );
};

export default Sidebar;
