// Video API service - Get session tokens for video calls
// TODO: Implement RTK Query endpoints

export const videoApi = {
  // getSessionToken: (appointmentId) => {},
  // endSession: (sessionId) => {},
};
