diff --git a/prisma/schema.prisma b/prisma/schema.prisma
index 609bb2a..a7a912e 100644
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -132,6 +132,7 @@ model Doctor {
   prescriptions     Prescription[]
   availabilities    DoctorAvailability[]
   slots             Slot[]
+  verifiedEhrRecords EhrRecord[]
 }
 
 enum SlotStatus {
@@ -240,6 +241,7 @@ model MedicalReport {
   appointment            Appointment?           @relation(fields: [appointmentId], references: [id])
   patient                Patient                @relation(fields: [patientId], references: [id])
   uploadedBy             User                   @relation("MedicalReportUploader", fields: [uploadedByUserId], references: [id])
+  ehrRecords             EhrRecord[]
 
   @@index([patientId, createdAt])
   @@index([appointmentId])
@@ -248,15 +250,22 @@ model MedicalReport {
 }
 
 model EhrRecord {
-  id            String       @id @default(uuid())
-  patientId     String
-  appointmentId String?      @unique
-  diagnosis     String?
-  notes         String?
-  aiSummary     String?
-  createdAt     DateTime     @default(now())
-  appointment   Appointment? @relation(fields: [appointmentId], references: [id])
-  patient       Patient      @relation(fields: [patientId], references: [id])
+  id                 String         @id @default(uuid())
+  patientId          String
+  appointmentId      String?        @unique
+  medicalReportId    String?
+  diagnosis          String?
+  notes              String?
+  aiSummary          String?
+  status             String         @default("DRAFT")
+  structuredData     Json?
+  verifiedByDoctorId String?
+  verifiedAt         DateTime?
+  createdAt          DateTime       @default(now())
+  appointment        Appointment?   @relation(fields: [appointmentId], references: [id])
+  medicalReport      MedicalReport? @relation(fields: [medicalReportId], references: [id])
+  patient            Patient        @relation(fields: [patientId], references: [id])
+  verifiedByDoctor   Doctor?        @relation(fields: [verifiedByDoctorId], references: [id])
 }
 
 model Prescription {
